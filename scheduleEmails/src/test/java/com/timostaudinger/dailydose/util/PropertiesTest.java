package com.timostaudinger.dailydose.util;

import org.junit.Test;

import static org.junit.Assert.assertNotNull;
import static org.junit.Assert.assertTrue;

public class PropertiesTest {
    @Test
    public void testProperties() throws Exception {

//        String redditUsername = Properties.get("reddit_username");
//        assertNotNull(redditUsername);
//        assertTrue(redditUsername.length() > 0);
//
//        String redditAppId = Properties.get("reddit_app_id");
//        assertNotNull(redditAppId);
//        assertTrue(redditAppId.length() > 0);
//
//        String redditClientId = Properties.get("reddit_client_id");
//        assertNotNull(redditClientId);
//        assertTrue(redditClientId.length() > 0);
//
//        String redditClientSecret = Properties.get("reddit_client_secret");
//        assertNotNull(redditClientSecret);
//        assertTrue(redditClientSecret.length() > 0);
//
//        String reddit_platform = Properties.get("reddit_platform");
//        assertNotNull(reddit_platform);
//        assertTrue(reddit_platform.length() > 0);

    }
}
